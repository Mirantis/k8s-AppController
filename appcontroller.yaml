apiVersion: v1
kind: Pod
metadata:
  name: k8s-appcontroller
spec:
  restartPolicy: Always
  containers:
  - name: kubeac
    image: nebril/k8s-appcontroller
    command:  ["/usr/bin/run_runit"]
    env:
      - # temporary only
        name: KUBERNETES_CLUSTER_URL
        value: http://10.0.0.1:8080
