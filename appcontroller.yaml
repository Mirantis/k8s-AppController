apiVersion: v1
kind: Pod
metadata:
  name: k8s-appcontroller
spec:
  restartPolicy: Always
  containers:
  - name: kubeac
    image: nebril/k8s-appcontroller:other_start
    command:  ["sh", "-c", "exec runsv /etc/sv/ac"]
